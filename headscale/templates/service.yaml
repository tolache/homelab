---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}-{{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
spec:
  type: NodePort
  selector:
    app: {{ .Chart.Name }}-{{ .Release.Name }}
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    nodePort: 30180
