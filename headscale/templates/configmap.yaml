---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-{{ .Release.Name }}-config
  namespace: {{ .Release.Namespace }}
data:
  config.yaml: |-
    {{- tpl (.Files.Get "config/headscale-config.yaml") . | nindent 4 }}